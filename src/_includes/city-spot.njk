---
layout: layout.njk
---

<section id="main" class="spot-page">
  <div class="inner">

    <h1 class="spot-title">{{ title }}</h1>

    {% if main_image %}
    <div class="spot-hero">
      <img src="{{ main_image | url }}" alt="{{ title }}">
    </div>
    {% endif %}

    <div class="spot-body">
      {{ content | safe }}
    </div>

    <div class="button-container">
      <a href="/generic_{{ city }}/" class="btn-fullstory">
        Explore more spots in {{ city | capitalize }}
      </a>
    </div>

  </div>
</section>