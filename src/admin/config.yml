backend:
  name: github
  repo: zotim4git/tripwithme    # 깃허브 user/repo
  branch: main
  base_url: https://tripwithme-nine.vercel.app
  auth_endpoint: api/auth

media_folder: "src/uploads"
public_folder: "/uploads"

collections:
  - name: "seoul"
    label: "Seoul"
    folder: "src/seoul_spot"
    create: true
    extension: "md"
   

    # 파일명 규칙 → slug 필드 값으로 파일 생성
    identifier_field: "slug"
    slug: "{{slug}}"

    fields:
      - label: "Title"
        name: "title"
        widget: "string"

      - label: "Slug (URL)"
        name: "slug"
        widget: "string"
        hint: "영어소문자-숫자-하이픈만 (예: seoul-sungbuk-cafe)"
        pattern: ['^[a-z0-9-]+$', '영어 소문자/숫자/하이픈(-)만 가능']

      - label: "City"
        name: "city"
        widget: "hidden"
        default: "seoul"

      - label: "Description"
        name: "description"
        widget: "text"

      - label: "Main Image"
        name: "main_image"
        widget: "image"

      - label: "Body"
        name: "body"
        widget: "markdown"
        